import SwiftUI

struct ProfileView: View {
	var body: some View {
		VStack {
			Text("Profile")
		}
		.frame(maxWidth: .infinity, maxHeight: .infinity)
		.background(.blue)
	}
}

#Preview {
	ProfileView()
}
